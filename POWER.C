#include<stdio.h>
int main()
{
int pow,num,i=1;
long int sum=1;
printf("\n enter a number");
scanf("%d",&num);
printf("\n power of a number");
scanf("%d",&pow);
while(i<=power)
{
sum=sum*num;
i++;
}
printf("\n %d to the power is %d:%1d,num,sum);
return 0;
}
