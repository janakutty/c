#include<stdio.h>
int main()
{

char s,j;
printf("\nEnter the first string");
scanf("%s",&s);
printf("\nEnter the second string");
scanf("%s",&j);
printf("\nThe concatenate of two string is %s%s",s,j);
return 0;
}
